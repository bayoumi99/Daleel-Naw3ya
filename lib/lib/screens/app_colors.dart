import 'package:flutter/material.dart';

class AppColors {
  // الألوان الأساسية
  static const Color primaryNavy = Color(0xFF292F91);
  static const Color accentBlue = Color(0xFF60A5FA);
  static const Color successGreen = Color(0xFF4ADE80);

  // ألوان الخلفيات
  static const Color lightBg = Color(0xFFF8FAFC);
  static const Color darkBg = Color(0xFF0F172A);

  // ألوان النصوص
  static const Color lightTitle = Color(0xFF1E293B); // أسود مزرق للنهاري
  static const Color darkTitle = Color(0xFFE2E8F0);  // رمادي فاتح جداً لليلي
  static const Color bodyText = Color(0xFF64748B);   // رمادي للنصوص الفرعية
}